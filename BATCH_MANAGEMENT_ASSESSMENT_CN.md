# 批量管理架构评估

本轮不实现批量多选。当前 `Card` 的长按会立即打开单书操作菜单，`Grid:updateItems()` 在翻页和刷新时会销毁并重建当前页卡片；如果把选中状态放进卡片，翻页就会丢失，如果直接改长按含义，又会破坏已经可用的加入、移出和排序操作。完整实现需要同时改变手势、标题栏、分页状态和 Store 事务边界，已经超出定向修复范围。

后续安全方案是把 `selected_paths` 放在 Grid 实例而非 Card 中：由独立菜单命令进入多选模式，轻点切换选择，翻页只重建视觉标记；标题栏明确提供“完成/取消”，取消不写 Store。最终由 Store 新增批量关系事务，先复制受影响分类，在内存中完成去重，再只 flush 一次，失败则整体回滚。首页只允许批量加入分类；分类页允许批量加入其他分类或移出当前分类，手动排序在多选期间禁用。还需在 Kindle 真机验证小触控目标、跨页选择提示和墨水屏反色残影后才能交付。
